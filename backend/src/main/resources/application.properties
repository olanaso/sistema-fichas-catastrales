# Configuraci髇 del sistema
spring.application.name=sistema-ficha-catastral

# Configuraci贸n de la base de datos PostgreSQL
spring.datasource.url=jdbc:postgresql://190.238.172.152:5400/postgres
spring.datasource.username=postgres
spring.datasource.password=bdgeo123
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuraci髇 de JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# Configuraci髇 JWT
jwt.secret=Mbv73Hd8zA92KdjfgLox29VsYdhffJ29Xksh2LsdQ89skdFw
jwt.expiration=3600000

# Configuraci髇 del servidor
server.port=8081
server.servlet.context-path=/api

# Configuraci髇 de logging
logging.level.org.catastro.sistemafichacatastral=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Configuraci髇 de archivos
upload.dir=uploads

# Configuraci髇 de validaci贸n
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always
spring.sql.init.continue-on-error=true

# Configuraci髇 de cach茅
spring.jpa.properties.hibernate.cache.use_second_level_cache=false
spring.jpa.properties.hibernate.cache.use_query_cache=false

spring.h2.console.enabled=true

# spring.main.allow-circular-references=true

# Configuraci贸n de prueba de spring security (deshabilitado para desarrollo)
# spring.security.user.name=admin
# spring.security.user.password=admin123

# Configuraci贸n de respuestas JSON
spring.jackson.default-property-inclusion=non_null
spring.jackson.serialization.fail-on-empty-beans=false
spring.jackson.deserialization.fail-on-unknown-properties=false

# Configuraci贸n de CORS (comentada porque se maneja en CorsConfig.java)
# spring.web.cors.allowed-origin-patterns=*
# spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
# spring.web.cors.allowed-headers=*
# spring.web.cors.allow-credentials=false

# Configuraci贸n de manejo de errores
server.error.include-message=always
server.error.include-binding-errors=always
server.error.include-stacktrace=never

# Configuraci贸n de Email SMTP
spring.mail.host=sa.sistema.edu.pe
spring.mail.port=465
spring.mail.username=desarollo@sistema.edu.pe
spring.mail.password=pass_33_2024
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.ssl.trust=sa.sistema.edu.pe

# Configuraci贸n de tokens
# 7 d铆as en milisegundos
jwt.refresh-token.expiration=604800000
# 15 minutos en milisegundos
password.reset.token.expiration=900000

# Configuraci贸n de la aplicaci贸n
app.frontend-url=http://localhost:3000
